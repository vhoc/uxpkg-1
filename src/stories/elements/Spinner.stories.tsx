import { Meta } from "@storybook/react/types-6-0";
import { Spinner } from "../../components/graphical/Spinner";

export default {
    title: 'elements/Graphical/Spinner',
    component: Spinner
} as Meta

export const Default = {
    args: {
        
    }
}